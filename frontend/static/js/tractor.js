/**
 * tractor.js - NOTE: This file is a placeholder.
 * The active tractor listing logic is in tractors.js (plural).
 * tractors.js is loaded by tractorview.html and handles:
 *   - GET /api/tractors (with optional ?location=&model=&min_price=&max_price= filters)
 *   - Rendering tractor cards in .tractor-grid
 *   - "Book Now" links to /book/<tractor_id>
 */
